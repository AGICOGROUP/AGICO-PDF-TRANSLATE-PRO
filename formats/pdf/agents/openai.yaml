interface:
  display_name: "PDF-TRANSLATE-PRO-ROUTER"
  short_description: "Routes PDFs by content, drawing type, and requested output mode."
  default_prompt: "Use $PDF-TRANSLATE-PRO-ROUTER to inspect my PDF. Default engineering drawings to Chinese-plus-foreign bilingual overlay, but use native-CAD when I explicitly require source text replacement."
